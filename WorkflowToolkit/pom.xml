<?xml version="1.0" encoding="UTF-8"?>
<project>
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.astoolkit</groupId>
	<artifactId>WorkflowToolkit</artifactId>
  
  <version>${BASE_VERSION}.${env.BUILD_NUMBER}-SNAPSHOT</version>
	<name>Workflow Toolkit</name>
	<packaging>swc</packaging>
	<properties>
		<sonar.branch>workflowtoolkit</sonar.branch>
	</properties>
	
	<parent>
    <artifactId>astoolkit</artifactId>
    <groupId>org.astoolkit</groupId>
    <version>${BASE_VERSION}.${env.BUILD_NUMBER}-SNAPSHOT</version>
  </parent>
  
  <dependencies>
  	<dependency>
  		<artifactId>ASToolkitCommons</artifactId>
	    <groupId>org.astoolkit</groupId>
	    <version>${BASE_VERSION}.${env.BUILD_NUMBER}-SNAPSHOT</version>
	    <type>swc</type>
  	</dependency>
  </dependencies>
</project>